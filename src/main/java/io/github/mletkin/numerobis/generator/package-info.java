/**
 * Builder generating classes.
 * <p>
 * Contains the classes that generate builder classes for annotated product classes.
 * Interface to thid pacakge are {@code CompilationUnit} objects generated with
 * the {@code JavaParser} package.
 * <p>
 * The class members in the compilation unit are sorted with the {@code Sorter} class.
 */
package io.github.mletkin.numerobis.generator;
