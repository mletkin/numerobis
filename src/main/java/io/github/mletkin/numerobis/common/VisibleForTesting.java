package io.github.mletkin.numerobis.common;

/**
 * Marker annotation for package visible components.
 * <p>
 * Used to inform the pmd checker that the component is intentionally package
 * visible to be used in unit tests.
 */
public @interface VisibleForTesting {
    // left intentionally empty
}
